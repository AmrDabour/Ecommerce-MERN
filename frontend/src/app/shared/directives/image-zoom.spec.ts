import { ImageZoom } from './image-zoom';

describe('ImageZoom', () => {
  it('should create an instance', () => {
    const directive = new ImageZoom();
    expect(directive).toBeTruthy();
  });
});
